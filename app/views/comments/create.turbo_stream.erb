<%= turbo_stream.append nested_dom_id(@commentable, @comment.parent_id, 'comments') do %>
  <%= render 'comments/comment', comment: @comment, commentable: @commentable %>
<% end %>
<%= turbo_stream.update nested_dom_id(@commentable, @comment.parent_id, 'new_comment'), ''%>